# 155 — Фікс порядку карток «Шок ціна» + модальне вікно вибору зображень

## Що зроблено
- Виправлено причину, через яку порядок карток міг не відображатися одразу на публічній сторінці:
  - `app/promotions/shock-price/page.tsx`
  - Додано `export const dynamic = 'force-dynamic'`, щоб порядок/контент брався з актуальної БД без застарілого кешу.
- Оновлено менеджер карток в адмінці:
  - `components/admin/admin-shock-price-manager.tsx`
  - Додано модальне вікно для дії `Обрати з сервера` з прев’ю зображень.
  - Додано пошук у модальному вікні.
  - Вибір зображення в модалці одразу підставляє URL картки.
- Збережено логіку ручного порядку:
  - кнопки `Вгору/Вниз`,
  - збереження через `Зберегти порядок і картки` у БД.

## Перевірка
- `npm run typecheck` — успішно.

## Примітка
- Після зміни порядку в адмінці обов’язково натискайте `Зберегти порядок і картки`, після цього публічна сторінка бере новий порядок одразу.
