# 023_2026-02-16_add_blog_routes_menu_and_images

## Дата
2026-02-16

## Що зроблено
- Додано пункт `Блог` у головне меню (`/blog`) та оновлено підпункт `Новини` на перехід у блог.
- Оновлено структуру контенту статей у `content/blog.ts`:
  - `slug`, `coverImage`, `thumbnailImage`, `content[]`.
- Реалізовано сторінку списку статей: `app/blog/page.tsx`.
- Реалізовано сторінки читання статей: `app/blog/[slug]/page.tsx`.
- На головній сторінці (`app/page.tsx`) у блоці `Блог: нові статті` додано міні-фото статей та переходи на сторінки статей.
- Для сторінок статей додано верхнє фото (cover) перед текстом.
- Оновлено документацію: `docs/project_status.md`, `docs/site_content.md`.

## Що потрібно зробити далі
- Підключити контент блогу до адмін-панелі та/або БД Supabase.
- Замінити тимчасові зображення на фінальні фото статей.
