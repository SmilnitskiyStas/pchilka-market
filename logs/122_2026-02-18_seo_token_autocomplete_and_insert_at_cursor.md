# 122 / 2026-02-18 / seo_token_autocomplete_and_insert_at_cursor

## Що зроблено
- Оновлено `components/admin/admin-seo-manager.tsx`.
- Додано підказки токенів під час заповнення полів:
  - `Meta title`
  - `Meta description`
  - `Canonical URL`
- Логіка роботи:
  - коли користувач вводить `[` у полі, відкривається список токенів;
  - список фільтрується за введеним текстом після `[`;
  - при натисканні на токен він вставляється в позицію курсора (або замінює поточний фрагмент `[...`).
- Додано токени: `[title]`, `[description]`, `[slug]`, `[Brand]`, `[domain]`, `[section]`, `[image_title]`, `[image_description]`.

## Перевірка
- Виконано `npm.cmd run typecheck`.
- Результат: без помилок.

## Що залишилось
- За потреби можна додати керування токенами через конфіг у БД (динамічний список токенів).
