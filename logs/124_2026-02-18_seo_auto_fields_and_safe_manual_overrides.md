# 124 / 2026-02-18 / seo_auto_fields_and_safe_manual_overrides

## Що зроблено
- Оновлено SEO-модель у `lib/seo-settings.ts`:
  - додано авто-генерацію SEO (`buildAutoSeoDraft`) для `title`, `description`, `canonical`, `robots`;
  - додано `getEffectiveSeo` для обчислення фінальних значень (ручні > авто);
  - залишено сумісність із поточним форматом правил.
- Оновлено runtime в `components/seo-runtime-loader.tsx`:
  - якщо в правилі поле порожнє, підставляється авто-значення;
  - canonical підтримує токен `[domain]`.
- Повністю оновлено `components/admin/admin-seo-manager.tsx`:
  - додано блок з поясненням, які поля формуються автоматично;
  - критичні поля за замовчуванням не редагуються вручну;
  - ручне редагування доступне через окремі перемикачі:
    - `Meta title` (override)
    - `Meta description` (override)
    - `Canonical` (override)
    - `Robots` лише в `Режим експерта`
  - у формі відображається live preview авто-значень для поточного маршруту;
  - токен-підказки працюють лише коли увімкнено ручне редагування відповідного поля;
  - список правил показує ефективні значення (з урахуванням авто-підстановки).

## Перевірка
- Виконано `npm.cmd run typecheck`.
- Результат: без помилок.

## Що залишилось
- Перенести правила в Supabase, щоб авто/override працювали серверно для SSR metadata.
- Додати окремі поля OG/Twitter і JSON-LD у цій самій моделі.
