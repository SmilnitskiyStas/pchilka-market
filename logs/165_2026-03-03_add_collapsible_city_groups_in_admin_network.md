# Task
Додати можливість приховувати/показувати адреси всередині груп міста в `Адмін -> Мережа -> Список магазинів`.

# Що змінено
Файл: `components/admin/admin-network-manager.tsx`

- Додано стан `collapsedCityGroups` для зберігання стану згортання по кожному місту.
- Додано утиліти:
  - `getCityGroupKey(city)`
  - `toggleCityGroup(city)`
- У хедері кожної групи міста додано кнопку:
  - `Згорнути` (коли група розкрита)
  - `Розгорнути` (коли група прихована)
- Контент зі списком адрес рендериться лише коли група розгорнута.

# Перевірка
- `npm run typecheck` — успішно.

# Результат
- Можна компактно працювати з довгими списками: групи міст займають мінімум місця у згорнутому стані.
