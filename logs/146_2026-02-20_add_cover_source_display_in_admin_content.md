# Task
Додати в адмінці (Контент -> список статей) зрозуміле відображення джерела `Cover`.

# Що зроблено
- Оновлено `components/admin/admin-content-manager.tsx`:
  - додано helper `getCoverLocation(...)`;
  - у розкритому рядку статті додано поле `Джерело`:
    - для `/img/...` та `http(s)` показується шлях/домен;
    - значення клікабельне (відкриває зображення в новій вкладці);
    - для `data:image/...;base64` показується текст:
      `Вбудоване зображення (base64 з адмінки)`.

# Результат
- Тепер видно, де саме лежить зображення (локальний шлях чи зовнішній URL).
- Для base64 є явна індикація, що це не файловий шлях.

# Перевірка
- `cmd /c npm run typecheck` -> success

