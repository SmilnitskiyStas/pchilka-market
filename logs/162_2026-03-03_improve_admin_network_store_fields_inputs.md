# Task
Покращити форму "Адмін -> Мережа -> Список магазинів":
1) зробити випадаючі підказки для міста;
2) виправити введення адреси з пробілами;
3) зробити випадаючі підказки для поля "Години роботи".

# Зміни
- Файл: `components/admin/admin-network-manager.tsx`
- Додано `cityOptions` (унікальні міста з поточного списку магазинів) через `useMemo`.
- Додано `workHoursOptions` (швидкі шаблони годин роботи) через `useMemo`.
- Додано `datalist`:
  - `store-city-options`
  - `store-work-hours-options`
- Підключено випадаючі підказки до інпутів:
  - поле міста через `list="store-city-options"`
  - поле годин роботи через `list="store-work-hours-options"`
- Виправлено проблему з пробілами у полі адреси (і інших текстових полях рядка магазину):
  - у `updateStore` прибрано нормалізацію `normalizeStore` на кожне натискання клавіші;
  - тепер зберігається сире значення вводу, а нормалізація виконується на етапі submit/load.

# Перевірка
- `npm run typecheck` — успішно.

# Результат
- Місто і години роботи заповнюються швидше через випадаючі підказки.
- Адресу можна вводити з пробілами без "склеювання" слів.
