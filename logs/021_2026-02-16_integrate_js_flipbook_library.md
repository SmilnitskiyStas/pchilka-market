# 021_2026-02-16_integrate_js_flipbook_library

## Дата
2026-02-16

## Що зроблено
- Інтегровано JS flipbook-бібліотеку `react-pageflip` у `promotions/catalog`.
- Оновлено `components/promotion-catalog-viewer.tsx`:
  - PDF сторінки рендеряться у зображення через `pdfjs-dist`;
  - сторінки передаються у `HTMLFlipBook`;
  - додано обробник `onFlip` для синхронізації поточної сторінки;
  - збережено fallback у сумісний `iframe`-режим, якщо `pdfjs` недоступний.
- Додано робочі кнопки навігації `first / prev / next / last`.
- Підтверджено успішну збірку проєкту після інтеграції.

## Що потрібно зробити далі
- За бажанням додати drag-перегортання та тонке налаштування параметрів `react-pageflip` під ваш стиль.
