# 056_2026-02-17_add_header_contacts_and_feedback_form

## Дата
2026-02-17

## Що зроблено
- Оновлено `components/site-header.tsx`.
- У верхній зоні хедера додано клікабельний телефон `tel:` для швидкого зв'язку.
- Додано кнопку `Зворотний зв’язок` у хедері.
- Реалізовано модальну форму зворотного зв'язку з полями:
  - ім'я;
  - телефон;
  - повідомлення.
- Додано базову валідацію форми (обов'язкові поля, мінімум 10 символів для повідомлення).
- Для MVP звернення з форми зберігається в `localStorage` (`feedback_requests`) і показується повідомлення про результат.
- Структуру меню в хедері збережено, меню перенесено на окремий нижній ряд у межах того ж блоку шапки.
