# 017_2026-02-16_fix_flipbook_load_worker_fallback

## Дата
2026-02-16

## Що зроблено
- Виправлено помилку завантаження flipbook на `/promotions/catalog`.
- Для `pdfjs.getDocument` додано fallback-завантаження без worker (`disableWorker: true`).
- Додано детальніший текст помилки в UI (виводиться причина з `error.message`).
- Підтверджено успішну збірку після змін.

## Що потрібно зробити далі
- Перезапустити dev-сервер і перевірити сторінку каталогу.
- Якщо помилка повториться, взяти точний текст нової помилки з UI/консолі.
