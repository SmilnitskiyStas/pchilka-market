# 010_2026-02-16_fix_pdf_viewer_and_global_header

## Дата
2026-02-16

## Що зроблено
- Виправлено сторінку `/promotions/catalog`, де каталог зависав на "Підготовка переглядача...":
  - прибрано `react-pdf` з клієнтського рендерингу;
  - реалізовано in-page перегляд PDF через iframe у форматі "книги" (2 сторінки на desktop, 1 на mobile);
  - залишено пагінацію `Попередні` / `Наступні` без переходу в нову вкладку.
- Додано серверне визначення кількості сторінок PDF через `pdfjs-dist` в `app/promotions/catalog/page.tsx`.
- Хедер з меню винесено в окремий компонент `components/site-header.tsx` і підключено глобально в `app/layout.tsx`.
- Прибрано дублювання меню з `app/page.tsx`.
- Видалено непотрібну залежність `react-pdf`.
- Оновлено документацію: `docs/project_status.md`, `docs/site_content.md`.

## Що потрібно зробити далі
- Додати обкладинки/thumbnail для архівних каталогів.
- Додати сторінку `/promotions/shock-price`.
- Додати сторінку `/promotions/buy-milka-win`.
