# 045_2026-02-16_fix_sticky_submenu_state_in_header

## Дата
2026-02-16

## Що зроблено
- Виправлено проблему, коли підменю залишалося відкритим після переходу на сторінку.
- Оновлено `components/site-header.tsx`:
  - компонент переведено в клієнтський режим (`use client`);
  - додано керований стан відкритого підменю (`openMenuLabel`);
  - підменю відкривається на hover/click і закривається:
    - після кліку по пункту підменю;
    - після кліку по пункту меню;
    - автоматично при зміні маршруту (`usePathname` + `useEffect`).
- Оновлено документацію:
  - `docs/project_status.md`;
  - `docs/site_content.md`.

## Що потрібно зробити далі
- За потреби додати повну клавіатурну навігацію по підменю (стрілки вгору/вниз, Enter/Space).
