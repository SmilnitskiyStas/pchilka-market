# 026_2026-02-16_fix_comment_wrapping_and_min_length

## Дата
2026-02-16

## Що зроблено
- Виправлено відображення довгих коментарів:
  - додано перенос рядків і довгих слів у блоці коментарів (`whitespace-pre-wrap`, `break-words`, `overflow-wrap:anywhere`).
- Для поля коментаря додано мінімальну довжину `10` символів:
  - `minLength={10}` у textarea;
  - додаткова перевірка в обробнику submit (`trim().length < 10`);
  - кнопка відправки вимикається, якщо умова не виконана.

## Що потрібно зробити далі
- За потреби додати текст помилки під формою, якщо коментар коротший за 10 символів.
